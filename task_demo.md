# Pola 展示：法兰轴套

用 revolve + extrude_cut 创建一个有生产价值的法兰轴套零件，导出 STEP。

**单位：米(m)** | **基准面：Front Plane(前视基准面)**

---

## 1. 创建零件

```
sw_create_new_part
sw_select_plane_or_face "Front Plane"
```

## 2. 旋转生成主体（法兰 + 轴套 + 通孔）

```
sw_draw_revolve_profile cx1=-0.001 cy1=0 cx2=0.071 cy2=0
    points=[[0,0.01],[0,0.015],[0.03,0.015],[0.03,0.035],[0.04,0.035],
            [0.04,0.02],[0.06,0.02],[0.06,0.01],[0.07,0.01],[0.07,0]]
```
几何（10点闭合）：通孔φ20 → 轴套外径φ30×30mm → 法兰φ70×10mm → 轴套φ40×20mm

```
sw_get_model_structure
sw_revolve_boss "草图1"       ← 失败试 "Sketch1"
sw_mass_properties            ← 预期 ~8e-5 m³
```

## 3. 打法兰螺栓孔（逐个手动，不依赖阵列）

**选法兰面打4个孔，φ6mm，PCD=φ55mm。孔位（在法兰面局部坐标）：**

```
# 孔1: center=(0, 0.0275)
sw_select_face_by_point x=0.035 y=0 z=0.035 entity_type="FACE"
sw_draw_circle radius=0.003 center_x=0 center_y=0.0275
sw_extrude_cut "草图2" thru_all=true

# 孔2: center=(0, -0.0275)
sw_select_face_by_point x=0.035 y=0 z=0.035 entity_type="FACE"
sw_draw_circle radius=0.003 center_x=0 center_y=-0.0275
sw_extrude_cut "草图3" thru_all=true

# 孔3: center=(0.0275, 0)
sw_select_face_by_point x=0.035 y=0 z=0.035 entity_type="FACE"
sw_draw_circle radius=0.003 center_x=0.0275 center_y=0
sw_extrude_cut "草图4" thru_all=true

# 孔4: center=(-0.0275, 0)
sw_select_face_by_point x=0.035 y=0 z=0.035 entity_type="FACE"
sw_draw_circle radius=0.003 center_x=-0.0275 center_y=0
sw_extrude_cut "草图5" thru_all=true
```

> 如果某步 extrude_cut 失败（草图名不对）→ 看错误信息里的"特征树中草图:"取真实名重试。不要多画圆。

## 4. 验证导出

```
sw_mass_properties            ← 体积略小于第一次
sw_export_to_format "STEP" "flanged_bushing"
sw_close_doc
```

---

# 验收

| 项 | 方法 | 预期 |
|----|------|------|
| 主体体积 | sw_mass_properties | ~7-8e-5 m³ |
| 法兰外径 | sw_measure 选法兰外圆面 | r≈0.035m |
| STEP文件 | 文件存在 | >20KB |

# Part of the code in this task is generated by human
